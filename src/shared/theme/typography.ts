import { StyleSheet } from 'react-native'

export const TYPOGRAPHY = StyleSheet.create({
  HEADLINE_LG: {
    fontSize: 32,
    fontWeight: '600',
  },

  BODY_MD: {
    fontSize: 16,
    fontWeight: '400',
  },
})
